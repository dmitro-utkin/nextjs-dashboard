/** @type {import('next').NextConfig} */

const nextConfig = {
  // experimental: {
  //   typedRoutes: true,
  // },
  sassOptions: {
    implementation: 'sass-embedded',
  },
};

export default nextConfig;
